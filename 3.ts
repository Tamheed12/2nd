let sir: string = "Nasir";

console.log(
  ` LowerCase ${sir.toLowerCase()}  
  \n UpperCase ${sir.toUpperCase()}
  \n TitleCase ${sir.charAt(0).toUpperCase() + sir.slice(1).toLowerCase()}`
);
